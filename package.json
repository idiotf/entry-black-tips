{
  "name": "entry-login-count",
  "module": "src/index.ts",
  "devDependencies": {
    "@types/chrome": "^0.1.24"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "watch": "bun build --watch --sourcemap=inline --no-clear-screen --minify --format=iife --outfile=build/dist/content.js src/content/index.ts & bun build --watch --sourcemap=inline --no-clear-screen --minify --format=iife --outfile=build/dist/injected.js src/injected/index.ts",
    "build": "bun build --minify --format=iife --outfile=build/dist/content.js src/content/index.ts && bun build --minify --format=iife --outfile=build/dist/injected.js src/injected/index.ts"
  },
  "type": "module"
}
